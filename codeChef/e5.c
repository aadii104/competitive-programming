#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(){
    char a[1000002];
    int i, test, flag, mid, flagp;
    scanf("%d\n", &test);
    while(test--> 0){
        flag = 1;
        gets(a);
        len = strlen(a);
        flagp = 0;
        for(i=0;i<len;i++){
            if(a[i      ])
        }
    }
}